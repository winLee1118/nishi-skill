# Citation Rules

Every source-backed answer should include at least one citation when available:

```text
[source_id | course | chapter | timestamp/page]
```

Prefer exact source metadata over vague references. If a chunk has `rights_status=unknown`, treat it as non-authoritative and do not quote it extensively.

